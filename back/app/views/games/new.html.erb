<h1>New game</h1>

<% form_for(@game) do |f| %>
  <%= f.error_messages %>
<!--
  <p>
    <%= f.label :owner_uuid %><br />
    <%= f.text_field :owner_uuid %>
  </p>
  <p>
    <%= f.label :player_uuid %><br />
    <%= f.text_field :player_uuid %>
  </p>
  <p>
    <%#= f.label :turn_expiration %><br />
    <%#= f.datetime_select :turn_expiration %>
  </p>-->
  
  
  <!--
  <p>
    <%= f.label :turn_speed %><br />
    <%#= f.datetime_select :turn_speed %>
  </p>
  -->
  
  <%= f.label :turn_speed %><br />
  
  
  <p>
    <%= f.label :turn_speed %><br />
	<p>Select the time you want until a player&#39;s turn expires. For example, the default selection below will give each player up to 1 day for each turn.</p>
	
	 
	<%= select_day(1, :field_name => :days) %> Days
	<%= select_hour(0, :field_name => :hours) %> Hours
	<%= select_minute(0, :field_name => :minutes) %> Minutes
	
	
    <%#= f.time_select :turn_speed, {:minute_step => 15} %>
  </p>
  
	<%
	#init
	map = Map.new
	maps = map.get_maps
	%>
	<% maps.each do |m| %>
		<%= f.radio_button :map_uuid, m[:uuid] %>
		<%= f.label m[:name] %><br />
	<% end %>

  <!--
  <p>
    <%= f.label :map_uuid %><br />
    <%= f.text_field :map_uuid %>
  </p>
  <p>
    <%= f.label :current_map %><br />
    <%= f.text_field :current_map %>
  </p>
  -->
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', games_path %>